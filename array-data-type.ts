// Array
let names: string[] = ["Coba", "Aja"];
let numbers: Array<number> = [1, 2, 3, 4, 5];

// Tuple
let biodata: [string, number] = ["Coba", 20];

// Enum
enum Color {
    Red,
    Green,
    Blue,
}

let c: Color = Color.Green;
console.log(c); // 1